# Changelog

本文件记录每个版本的更新内容。**发布新版本时**：

1. 递增根目录 `VERSION` 文件（如 2.0.0 → 2.1.0）
2. 同步 `__init__.py` 里的 `__version__`
3. 在本文件顶部添加一条 `## [新版本号] - 日期`

用户可在节点工具栏「🔄 更新」检查到新版本。

## [2.12.3] - 2026-09-13

### 变更

- **项目改名为 `Tk Toolkit`**（作者「时运Tk」的品牌）。原显示名 "Anima Toolkit" 与**训练**工具链
  `AnimaLoraToolkit`(66★) 撞脸 —— 搜 "anima toolkit comfyui" 时对方排第一，AI 容易把我们归错类；
  另有一个同名 fork `bfftp0502/comfyui-anima-toolkit`。

  **改名范围刻意只动"对外身份"层**（零风险，老用户完全不受影响）：

  | 改了 | 没改（改了会出事的） |
  |---|---|
  | Registry `DisplayName` → `Tk Toolkit` | **Registry 节点 id** 仍是 `anima-toolkit`（换 id = 建新节点、重新排队审核，还会在 registry 留一个重复节点） |
  | README 标题 / 英文摘要 / 安装块 / 图标 alt | **GitHub 仓库地址**不变（老用户更新链硬编码了旧仓库名；且旧名释放后有被抢注的供应链风险） |
  | `pyproject` description + `authors` | **`NODE_CLASS_MAPPINGS` 类名**（已保存的工作流 JSON 存的就是这些字符串） |
  | `llms.txt` / `AGENTS.md` / `CLAUDE.md` / 两个模块头注释 | **`anima_` 文件与模块前缀**（更新链白名单就是 `path.startswith("anima_")`，改了等于重踩漏发 bug） |
  | | **节点显示名**保持 `TK xxx`（38 处代码 + 34 处测试断言，纯 churn 且无检索收益） |

  **召回不掉**：`Anima` 关键词保留在 `description` 里 —— 实测 registry 搜索**会索引 description**
  （搜 `danbooru` 能搜到我们，而 "danbooru" 并不在节点名里），所以从 "Anima Toolkit" 改成 "Tk Toolkit"
  不会丢掉 `anima` 这个查询词。

- **作者署名**：`[project].authors = [{ name = "时运Tk" }]`。**刻意没写 email** —— 一提交就进公开仓库
  历史、无法撤回；要公开再补 `email = "..."`（PEP 621 允许只填 name）。

## [2.12.2] - 2026-09-13

### 新增

- **可被 AI / 搜索检索到的元数据**（目标：让 AI 优先检索到这个插件）：
  1. **Registry 图标**：新增 `screenshots/icon.png`（400×400，由 `tools/make_icon.py` 生成，
     取自面板自身配色），并在 `pyproject.toml` 填上 `Icon`。此前 registry 上的 `icon` 是空的 ——
     registry / ComfyUI-Manager 列表里没有缩略图，直接影响点击与发现率。
  2. **`llms.txt`**（仓库根）：面向 AI/LLM 的机器可读摘要 —— 一句话定位、安装命令、全部节点清单、
     关键词、以及"别认错"说明。新兴的 LLM 友好约定，成本极低。
  3. **README 头部补安装块与关键词**：新增 `## Installation / 安装`（Manager 搜索名 + `git clone`
     命令 + registry 链接 + 依赖指引）。此前前 4000 字符里 `install` 出现 **0 次** ——
     需要"替你安装"的 agent 找不到可执行指令。关键词补上
     `comfyui-manager` / `tag-autocomplete` / `chinese-tag-autocomplete` / `workflow-automation`。
  4. **同名项目区分声明**：`AnimaLoraToolkit`(66★) / `AnimaLoraAtelier` 是**训练**工具链，
     搜 "anima toolkit comfyui" 时它排第一。README 与 `llms.txt` 都写明本项目是**推理侧**
     工具包（加载/管理/检索 LoRA，不训练），避免 AI 把我们归错类。

> 遗留：registry 上的 `category` / `tags` 仍是空的 —— comfy-cli 的 `[tool.comfy]`
> **只认 `PublisherId` / `DisplayName` / `Icon` / `includes`**（实测其 `config_parser.py`），
> 这两个字段只能在 registry 网页端补，不是代码问题。三个版本的审核状态仍是 `Pending`
> （`latest_version` 因此为空），审核通过前 Manager 的注册表安装路径不会列出来。

## [2.12.1] - 2026-09-13

### 修复

- **内置更新链漏发 `services/` 与随包词典**。发布白名单曾把 `data/` 与 `services/` 整个排除，
  后果有两个：
  ① 新词典永远下发不到 → 中文联想在老用户机器上表现为"代码在、索引不在"，**不报错但没用**；
  ② `services/` 不下发 → `__init__.py` 拆分后 `from .services.github_update import …` 是硬依赖，
  点更新会拿到新 `__init__.py` 却拿不到 `services/` → **ImportError，插件整个加载不了**。
  现在 `services/` 全量下发；`data/` 改为**逐文件白名单**（只放行随包词典，
  `prompt_library.json` / `batches/` / 账号 / 缓存等用户状态永不覆盖）；
  别名索引改放插件根目录 `anima_alias_index.json`，这样**连旧版更新链**都能把它下发下去。
- 本次同时把 2.12.0 的更新链修复做成正式版本 —— 只装 2.12.0 的用户点「更新」即可拿到根目录词典
  （中文联想立刻生效）；更新链本身要等下一次完整安装才会落地，不影响功能。

## [2.12.0] - 2026-09-13

### 新增

- **TK 提示词卡片 · 中文角色名联想**：②区输入中文能直接找到角色，例如
  `望`/`橘望` → `nozomi (blue archive)`、`妃咲`/`龙华妃咲` → `kisaki (blue archive)`、
  `日奈` → `hina (blue archive)`、`星野` → `hoshino (blue archive)`。
  数据来自两层**可验证**来源，不含任何 LLM 编造：
  ① 社区词典与官方译名（`data/danbooru_tags_zh.json`、原 CSV 的「关键词」字段）；
  ② 从 Civitai LoRA 元数据里挖出的真实配对（中文标题 ↔ `trainedWords` 里的 danbooru 标签，
  共 2638 条，每条都带证据）。
  另引入 **AnimaDex 角色表**（36,480 个角色 / 3,702 部作品），所以输入作品名
  （如「碧蓝档案」）也能直接列出该作品角色。

### 修复

- **内置更新链「只更新代码、不更新词典」**（老用户会静默失效）：发布白名单曾把 `data/` 与
  `services/` 整个排除。后果有两个，第二个更严重：
  ① 新词典永远下发不到 → 2.12.0 的中文联想在老用户机器上表现为"代码在、索引不在"，**不报错但没用**；
  ② `services/` 不下发 → `__init__.py` 拆分后 `from .services.github_update import …` 是硬依赖，
  老用户点更新会拿到新 `__init__.py` 却拿不到 `services/` → **ImportError，插件整个加载不了**。
  现在 `services/` 全量下发；`data/` 改为**逐文件白名单**（三个随包词典下发，
  `prompt_library.json` / `batches/` / 账号等用户状态永不覆盖）；别名索引改放插件根目录的
  `anima_alias_index.json`，这样**连旧版更新链**都能把它下发下去。
- **中文查询的排序错误**：以前中文命中别名排在「英文标签子串」和「说明字段包含」之后，
  于是「日奈」「星野」「白子」都被同名的别作品角色挤掉。现在中文查询优先走别名表：
  **精确 → 中文名尾缀（名而非姓）→ 前缀 → 别名包含**，英文子串与说明只做兜底。
- **「望」命中 `telescope`**：说明字段里的「望**远镜**」曾让天文器材标签排在角色前面 ——
  中文查询不再扫描英文说明，只在别名表完全没有结果时才回退。
- **`tests/tools/run_offline_like_ci.py` 报告失败时自身崩溃**：失败分支里的 ❌ 在 GBK 控制台上
  触发 `UnicodeEncodeError`，把真正的报错盖掉（与 `ai_verify.py` 同一个坑），已改为纯 ASCII。

### 性能

- **中文查询不再扫描 20 万条英文说明**：改为预建索引，首次中文查询从「秒级」降到毫秒级；
  索引在插件加载时**后台线程预热**，不占用用户首次输入的时间。
- **英文查询 350ms → 60ms**：标签扫描（廉价子串）与别名查找（O(1) 精确 / O(log n) 前缀二分 / O(1) 尾缀）
  分开执行，不再对每个条目逐个比别名。

### 变更

- **插入提示词默认转义括号**：ComfyUI 把裸括号当权重语法（`(x)` 会加强 x），
  要拿到字面标签 `nozomi (blue archive)` 必须写 `nozomi \(blue archive\)`。
  与 D 站画廊的「转义括号」同一套语义（反转义后再转义，重复插入安全）。
  ②区工具栏新增 **`括号转义 开/关`** 按钮，可随时切回裸括号；该选项保存在本地 UI 状态里。

## [2.11.0] - 2026-09-13

### 新增

- **TK D站画廊 · 真瀑布流**：布局从「CSS Grid 行跨」换成**列填充 + 超宽图跨 2~3 列**的绝对定位瀑布流。
  旧实现有两个硬伤：卡片宽度恒等于列宽（1298px 宽的网格 8 列 → 每张只有 156px，节点再宽也只是列数变多、图越小），
  且同一行里高矮不一会留下成片空白（实测 48 张卡在 1298×1438 里**底部空 106px**、行间还有洞）。
  实测新实现：**底部余量 106px → 9px、右侧余量 3px、重叠 0**。
- **TK D站画廊 · 自适应张数（新默认）**：「每页图片数」新增**自适应**档 ——
  按节点宽高算出「列数 × 可视行数」即刚好填满一屏的张数（夹到后端上限 48）；
  拖动节点改变大小会自动重算；整页铺不满时自动收掉底部空白。
  旧的 12/24/48 固定值仍可选，已保存过固定值的节点不受影响。
- **TK D站画廊 · 随机发现（三档）**：工具栏新增 `热门随机`（评分 ≥100）/ `优质随机`（≥50）/
  `高收藏随机`（收藏 ≥30），均为 `order:random` 配**质量地板**，避免裸随机倒进没人贴过的冷门图。
  带 `force=1` 绕过 30 秒缓存（否则"随机"会给出完全相同的一批）；`换一批`会检测是否真的换出新图，
  池子见底时明确提示而不是假装换过；内容标签与随机池无交集时自动退化为纯随机并说明。
- **Outputs · 生成完即后台更新**：新增后端轻量探测 `GET /anima/gallery/fresh`（只遍历输出目录文件名并 stat，
  不解析 PNG、不返回 16.5MB 索引），并监听 ComfyUI 的 `executed` / `execution_success` 事件。
  **不在 outputs 页面时也会在后台把新图准备好**，切回来立即可见
  （旧实现只在窗口获焦/切页时扫描、隐藏时直接 return —— 正是"不进页面就不更新"的原因）。
- **只读验证 CI**（`.github/workflows/ci.yml`）：Python 编译 → `ai_verify` → 离线 pytest →
  JS 测试 → panel `tsc` → panel 构建，每个环节独立 step、失败环节一眼可见。
  权限只有 `contents: read`、不做任何提交，与负责提交 `app/` 的 `build-app.yml` 完全分开；
  push 到 main 与 PR 都跑，pip/npm 双缓存。
- **测试分层与一键入口**：`tests/` 按「正式回归 / 需要真实环境 / 一次性脚本 / 工具」分层
  （unit 23 / js 3 / integration 57 / smoke 4 / repro 12 / tools 5），
  清单由 `tools/classify_tests.py` 生成并由 `test_layer_manifest.py` 守卫；
  新增 **`python tests/run_tests.py`**：本地跑的等于 CI 跑的那一套。
- **ComfyUI Registry 元数据**：新增 `pyproject.toml`（按当前官方规范）与
  `requirements.txt`（必需依赖）/ `requirements-optional.txt`（`playwright`、`llama-cpp-python`）。
  **刻意不声明** `torch` / `numpy` / `PIL`：由 ComfyUI 自带，重复声明会引发版本冲突。

### 修复

- **排序不占计数槽（长期潜伏 bug）**：`order` 曾被错误地算作免费 metatag，
  导致「计数标签超限时自动移除排序」的分支与其提示条**变成死代码**
  （回归 `test_danbooru_gallery_interactions.py` 自 2026-09-07 起一直失败，却被记成"预存在失败、非本轮引入"）。
  现已修正为占 1 槽，前后端语义一致。
- **搜索框与筛选面板重复拼词**：搜索框里若残留 `rating:g` / `-filetype:mp4`（历史写入），
  会与筛选面板各拼一次 → 白占计数槽，还会让"退化重试"拿到一模一样的查询而形同没重试。
  现已让筛选面板成为这些 token 的唯一 owner（与 `order:` 已有的规矩一致），并加大小写不敏感去重。
- **Prompt 库镜像的备份链曾整体失效**：`PROMPT_LIBRARY_BACKUP_PATHS` 在**导入时**从
  `PROMPT_LIBRARY_PATH` 派生并固化成常量，而 `save_snapshot` 用的是当前值 ——
  一旦数据目录被重定向（测试隔离必做），主文件写新位置、**备份却写旧位置**，
  而 `load_snapshot` 又去新位置找备份。现改为按当前路径**现算**。
- **画布上出现两个画廊导致按钮重复**：运行目录 `web/` 里放过备份文件，会被 ComfyUI
  当扩展一起加载、`registerExtension` 跑两次。备份已移出 `web/`，并立规矩：
  `web/` 是会被服务出去的目录，任何 `.js` 备份都不能放在里面。
- **`ai_verify.py` 只在有 FAIL 时崩溃**：其中用了 `❌` / `⚠️`，默认 GBK 控制台下 `print` 会抛
  `UnicodeEncodeError` —— 恰好在你最需要它报告失败的时候。已替换为 ASCII 标记。
- **11 个"脚本式"测试此前根本没在跑**：模块顶层写 `assert` / `sys.exit()`，
  pytest 要么收集期 `INTERNALERROR`，要么**导入时执行且失败不会被报告（静默假绿）**。
  已全部改造为可被收集的正式用例（含"安全更新 ZIP"的回归）。
  另修正 3 处**从来没通过过**的断言（引用不存在的变量、断言写死整串 HTML）。

### 变更

- **`__init__.py` 拆分第一阶段**：2657 → **2448 行**，把 **GitHub 自动更新链**拆到
  `services/github_update.py`（边界最清晰、且有独立回归兜底）。
  `__init__.py` 保留薄适配层与两条路由（所有 `/anima/*` 的唯一入口要集中），
  会话与目录用显式注入避免循环导入；路径穿越校验与逐文件回滚**逐行照搬、未做任何简化**。

### 工程

- `VERSION` 成为**唯一真源**：`__init__.py` 的 `__version__` 改为运行时读它；
  `ai_verify.py` 新增版本一致性检查（VERSION / `__version__` / README / CHANGELOG / pyproject 五处，任一处漂移即 FAIL）。
- 新增 `tools/bump_version.py`：发版时一处改、五处同步，避免手改漏项。
- `docs/FEATURES.md` 清掉残留的写死版本号（v0.4）。
- README 第一屏重写：英文一句话定位 + 英文 TL;DR + 「这个项目解决什么（10 秒版）」表格，
  **中文仍是主体**、详细文档全部保留；顺带修掉第 8 节标题写"十一"、正文写"十二"的不一致。

## [2.10.0] - 2026-09-12

### ⚠️ 破坏性变更

- **`TK 相机控制` 节点已退役移除**，由 **`TK 可动素体相机`** 取代。旧工作流里的该节点会显示为
  「缺失节点类型」，请删除后改用素体相机：机位参数（`pos_x` / `pos_y` / `pos_z` / `roll` / `fov`）
  与输出端口（`camera_prompt` / `camera_meta`）语义一一对应，预设与权重算法同为 `CameraControlCore`，
  换节点即可，无需重新调参。
- 前端 `web/js/anima_camera_control_widget.js` 一并删除（节点已不存在）。
- `anima_camera_control.py` **模块本身保留**：素体相机通过 `CameraControlCore` / `DEFAULT_CONFIG_JSON` /
  `CUSTOM_PRESET` / `all_preset_names` 复用它，删除该文件会导致素体相机失效。

### 修复

- **LoRA 探索 · 「选择版本」下拉被裁掉大半**：`.version-dropdown` 是 `position:absolute; bottom:100%`
  （向上弹），而卡片链路上有 3 层 `overflow:hidden`（`.card-body` → `.card` → `.vs-card-wrap`），
  浮层弹出即被裁切。现于展开时临时解除「浮层 → 滚动容器」之间所有祖先的裁剪（含 `.card` 的
  `content-visibility:auto`）并把卡片抬到 `z-index:40`，收起时逐级还原；上方空间不足则自动翻转为
  向下展开。滚动容器自身保留裁剪，避免整页跟着滚动。

### 文档

- README 功能总览配图全面换新（节点浏览窗 / LoRA 组 / 本地 LoRA 管理 / LoRA 探索 / 下载展开 / Outputs），
  并补上素体相机配图；`TK 节点系列` 由十二个改为十一个。

## [2.9.0] - 2026-09-12

### 新增

- **TK 批量 LoRA 加载器 · 触发词输出改造**：`trigger_words` 输出改为直接输出**本节点已激活（权重非 0）LoRA 的触发词**，且与「是否加载成功」解耦——文件缺失但被激活的 LoRA 也会带出触发词。禁用项由面板以 `<lora:name:0.00>` 写入，既不加载也不取词。
- **触发词总开关**：节点工具栏新增「输出触发词 k/N」勾选框（对应输入项 `output_trigger_words`，默认开启）。关闭后该输出为空字符串，**LoRA 加载不受任何影响**。
- **触发词后端持久化**：节点面板把「LoRA → 触发词」推送落盘（`data/lora_trigger_words.json`，端点 `POST /anima/lora_trigger_words`）。执行时按「节点推送的持久表 > 内存 bridge > anima_bridge.json」解析，不再依赖面板是否推送过 bridge，刷新/重启后依然可解析。名称写成全路径 / 去扩展名 / 纯文件名都能命中同一份触发词。

### 修复

- **`_find_lora_path` 模糊匹配分支崩溃（重要）**：该分支调用了从未定义的 `base()` → `NameError`。真实文件名带 ` (1)` 之类后缀时（例如 `ITEFU-KREA-V1 (1).safetensors`）必然走到这里，后果是：`/anima/lora/info` 返回 500（**触发词因此永远提取不到**）、「验证标签」报错，**并且节点执行会直接失败**。已修复并补回归测试 `tests/test_batch_lora_trigger_words.py`（18 项，离线可跑）。
- **D 站标签分类器 · 权重归一化合并**：运行目录里存在一版比仓库更细的权重归一（空值 ≠ 0、0.05 步进、夹到 0–2），但它**丢掉了仓库版的关键行为**——旧工作流缺失权重字段被恢复成 `0` 时必须写回 `1.0`（否则节点按 `(tag:0)` 执行、该分类 Tag 被静默丢弃），项目自带 UI 测试正是这么判定的。现已把两者合并：空值 / 非数字 / `≤0` 一律归一为 `1.0` 并写回 + 标记工作流已变更，其余值按 0.05 步进夹到 `0.05–2`。`verify_danbooru_tag_getter_ui.py` 通过。

### 性能（画布拖动掉帧专项，两轮）

- **卡片区渲染窗口化**：`TKPromptCards` 原先一次性渲染整库（实测 984 张卡 ≈ 1 万个 DOM 元素 / 4 千个控件，而卡片区仅 300px 高），现改为首屏 60 张 + 滚动补渲染（保留滚动位置、切分类/搜索时收回首屏）。卡片区 DOM 由 ~10052 降到 ~601。
- **隐藏原生 widget 不再留残影**：改为就地写 `options.hidden`（原先整体替换 options 会让前端判定失效），消除 12 个整宽遗留 `<canvas>` 行：canvas 12 → 1 张、占用 10.4MB → 0.8MB。
- **新增 `web/js/anima_canvas_perf.js` 节点级渲染裁剪**：为每个节点写入精确的 `content-visibility` + `contain-intrinsic-size`（视口外节点跳过渲染）并做图层提升。Chrome Tracing 实测每帧 `Layerize` 6.8ms → 3.8ms（−44%），平移帧时 −12% ~ −48%（取决于离屏节点比例）。
- 合计：工作流图容器 DOM 21001 → 12063（−43%），拖动平移帧时最高下降约 48%。不想用时可在控制台执行 `localStorage.setItem('tk_canvas_perf','0')` 后刷新关闭裁剪。

## [2.8.1] - 2026-09-11

- **URL 下载 · 支持 civitai.red 镜像链接**：粘贴 `civitai.red/models/...` 链接此前报「无法解析」——URL 解析只认 civitai.com。现已统一为共用解析函数，civitai.com / civitai.red 及未来镜像域名均可；「手动匹配」与「添加 LoRA」入口同步修复。报错信息也会显示完整链接便于排查。纯前端改动，刷新浏览器（Ctrl+Shift+R）即可生效。

## [2.8.0] - 2026-09-11

- **本地 LoRA 管理 · 左栏「按底模」筛选**：新增可展开的「按底模」面板，自动汇总本库已匹配 LoRA 的基座（Anima / Krea 2 / SDXL 等）并显示各自数量，点击即筛选、再点取消；未匹配的归入「未匹配」项；与「分类」筛选可叠加，网格/列表视图均可用，选择会记住。纯前端改动，刷新浏览器（Ctrl+Shift+R）即可生效。

## [2.7.2] - 2026-09-11

- **LoRA 探索 · 基座筛选新增 Krea 2**：基座下拉加入「🟠 Krea 2」，可直接按该基座探索 Civitai 上的 Krea 2 LoRA（筛选值已实测有效，最高下载 3 万+）。纯前端改动，刷新浏览器（Ctrl+Shift+R）即可生效。

## [2.7.1] - 2026-09-11

- **Outputs · 左侧目录树消失修复**：2.7.0 的 Gallery 索引模式接管列表后，启动流程跳过了目录句柄恢复，导致刷新页面后左侧文件夹树消失、不得不重新「选择目录」（且会触发全量重扫）。现已修复：有历史授权时句柄自动恢复、左侧树照常显示、增量扫描/删除等文件操作不受影响；无授权时目录树直接由 Gallery 索引的相对路径生成（纯数据、秒出、无需重新授权）。纯前端改动，刷新浏览器（Ctrl+Shift+R）即可生效。

## [2.7.0] - 2026-09-11

### 新增

- **Outputs · 元数据后端化（按钮随卡片秒现）**：后端新增图库索引（`/anima/gallery/manifest` 与 `/anima/gallery/meta`）——扫描、PNG 参数解析、LoRA 提取、宽高读取全部在本地后端完成（实测 3571 张约 13 秒建库，增量更新 0.4 秒），索引含正向 prompt 全文与 LoRA 列表（支持 ComfyUI UI/API 双格式与「文本拼接 / tag 库节点运行时组装」类工作流的正向链路回溯）。前端打开 Outputs 时一次拉取清单，**列表、预览图、全部按钮同时出现**；完整工作流 JSON 仍按需获取。索引首次构建期间界面照常可用，构建完成后自动切换。后端不可用时自动回退旧管线。

## [2.6.0] - 2026-09-11

### 新增

- **Outputs 预览图改由本地后端直出（秒开 + 高清）**：卡片图片不再由浏览器逐张读盘、解码原图、缩放、编码、存浏览器缓存，改为直接引用后端 `/anima/thumb` 生成的 **512px 高质量 WebP**（LANCZOS，落盘长期复用，按内容自动失效）。单张从约 1.4MB 原图解码降为 30KB 级小图，**首屏与滚动加载接近瞬时**，预览图不再模糊；后端不可用或图片不在 output 目录时自动回退旧管线，升级无感知。需重启 ComfyUI 生效。

### 修复

- **Outputs：浏览图片时点击目录树/切换栏目无响应** —— 元数据按需加载到位后每 250ms 触发一次网格全量重建，浏览期间主线程被反复占住（点击目标还常落在重建窗口里被销毁）。现改为逐卡增量同步按钮与信息行，图片节点不动；实测滚动期间点击响应 6ms、长任务 0 个。

## [2.5.1] - 2026-09-11

### 修复

- **Outputs：卡片上的「复制 LoRA 标签」按钮消失** —— 三层原因叠加：虚拟网格的内容签名只看缓存条目数（元数据是同 key 覆盖，数量不变 → 按需加载到 LoRA 后不重建，按钮停留在旧 HTML）；全库元数据补齐/扫描写回会无条件覆盖缓存条目，把已提取的 LoRA 列表清空（IndexedDB 不存该字段）；可见区加载的早退判据只看「有没有缓存」，清空后不再补提取。修复：签名改用元数据内容版本号；覆盖写入时按「工作流指纹」沿用已提取结果；早退判据改为「已提取或无需提取」；元数据读取加 3 秒超时 + 退避重试（实测 IndexedDB 偶发悬挂 >9 秒）。

### 新增

- **后端缩略图直出端点 `/anima/thumb`（第一步，前端暂未接入）**：为下一版「秒开 + 高清预览」打底。512/768 两档 WebP 落盘缓存（LANCZOS），缓存键含源文件 mtime/size（图一变自动失效），带浏览器强缓存头；路径做严格目录校验。生成走旁路线程且并发限流，不影响出图。此版本前端仍走旧管线，升级无感知。

## [2.5.0] - 2026-09-11

### 性能

- **本地 LoRA 管理：切页与「URL 下载」弹窗卡顿修复** —— 此前网格列表每次都全量重建 DOM（CDP 实测 1200 个 LoRA 下单次主线程阻塞约 500ms），且每次切页、搜索、匹配进度更新都重复付出；大量 LoRA 时打开下载弹窗后页面接近无响应。现改为：
  - 列表分片渲染：首屏渲染 150 张卡片，滚动到底自动追加（内容与全量渲染一致，滚动位置不丢）；
  - 扫描/匹配期间的高频状态更新合并为每 250ms 至多一次重渲染（此前每匹配一个文件就全量重建一次列表）；
  - 后端分类快照与本地一致时不再触发重渲染；
  - 全库元数据补齐（「关联出图」统计）延迟到页面激活 1.2 秒后进行，不与首帧和用户交互抢主线程；
  - 列表视图折叠分类不再把内容解析进 DOM；网格预览图请求尺寸对齐实际显示尺寸（480→240，解码内存减半）。
  - 实测：切换页面主线程阻塞 521ms → 0ms，弹窗 15ms 打开。纯前端改动，更新后刷新浏览器（Ctrl+Shift+R）即生效，无需重启 ComfyUI。

## [2.4.0] - 2026-09-10

### 修复

- **LoRA 探索：NSFW 筛选失效** —— 此前只在选择「仅 SFW」时传 `nsfw=false`，选「全部」时**省略**该参数；而 C 站 API 省略 `nsfw` 等价于仅返回 SFW，导致「怎么选都是 SFW」。现在始终显式传参（全部 → `true`，仅 SFW → `false`）。
- **LoRA 探索：空状态反复闪烁、有结果却显示为空** —— 远程已按同一关键词检索后，本地不再重复文本过滤（新增 `resolvedQuery` 判定，本地过滤只用于输入防抖窗口内的即时反馈）；空状态节点改为幂等复用，不再残留压在卡片上方。
- **LoRA 探索：点「加载更多」后翻页栏消失** —— 底部分页区与 `hasMore` 解耦（`updatePager()`）：只要抓取过（`page>0`）就保留分页区，仅在没有下一页时隐藏「加载更多」。
- **LoRA 探索：窗口缩放整屏闪烁** —— 引入布局签名，签名不变不重建网格；触底自动加载增加「内容未超过视口不自动加载」守卫，避免反复触发。
- **本地 LoRA 扫描：哈希性能与取消反馈** —— 补回原生快速路径（文件 ≤256MB 且支持 `crypto.subtle` 时整文件 digest；大文件仍分块 + Worker），并带取消检查；取消扫描时 `progressHide()` 复位状态并提示「⏹ 扫描已取消」。
- **Outputs 图片库：回退瀑布流布局** —— 瀑布流改动会导致图片无法加载、页面按钮无响应，已回退为固定比例网格（稳定版），浏览恢复流畅。相关实现已单独归档，待修复后重新引入。

### 新增

- **C 站线路可配置**：设置面板新增「C 站线路」（默认官方 `civitai.com` / 镜像站 / 自定义域名）与只读 API Key；模型与作者链接随线路变化。部分网络下官方域名不可达时可切镜像站。
- **排序「📊 赞比（本地排序）」**：按点赞/下载比值降序排列已加载结果；该本地排序项不会被当作 API 排序参数下发。
- **质量筛选「🎯 高质（下载≥250 且 赞比≥5%）」**：显式选中才生效，不会默认静默过滤结果。
- **空状态可操作化**：直接说明「C 站返回 N 条，被哪一级筛选筛掉」，并提供一键清除导致空结果的筛选条件。

## [未发布] - 2026-09-07

### 修复

- **TK 3D Body Camera 3D 引擎不可用**：此前 Three.js 只从外部插件 `Comfyui-Anima_camera_angle` 的路径加载，未安装该插件的机器上节点永远停留在“正在加载本地 3D 引擎…”。现在插件自带 `web/js/three.module.js`，优先从本插件加载，外部插件路径仅作兼容兜底。
- **TK 批量 LoRA 加载器触发词污染**：0.00/0.00 权重的 LoRA 不再加载（跳过空操作）；Bridge 查不到触发词时输出空串，不再回退到文件名（避免把文件名当触发词混进提示词）。
- **TK 空Latent 图像**：节点新增 `width`/`height` 两个 INT 输出口，颜色链等下游可直接读取尺寸。

### 改进

- **TK Danbooru Tag Getter**：修复旧工作流恢复后分类权重变成 `0` 的问题；`0` 会按中性权重 `1.0` 处理，前端最小可调值为 `0.05`，避免输出 `(tag:0)`。

- **TK String Router**：新增工作流可保存的输出槽位顺序；多选模式可在节点内拖拽行交换顺序，无需改动连线。每行默认优先显示画布上的自定义节点名与输出名称，也可切换原始节点名，并保留自定义别名；拖拽时增加落点高亮、状态过渡和 reduced-motion 兼容。
- **TK Danbooru Tag Getter**：`natural_language` 输入升级为统一 Prompt 入口；没有分类包时会按本机 Danbooru 数据库自动归类已知 Tag，未知句子由“保留自然语言”控制；每个分类可独立设置 `0.0–2.0` 权重，已有 Tag 权重会按分类权重相乘。分类索引优先读取 Packer 的 xlsx（直接解析 XML，避免 openpyxl 启动开销），找不到时回退随插件 CSV；旧双输入工作流继续兼容。

## [2.3.0] - 2026-08-29

本次版本对应最近推送的 LoRA 子目录功能。

### 新增

- **本地 LoRA 网格视图**：本地管理支持列表 / 网格切换;网格卡片显示大预览、模型信息、版本、下载/点赞数和分类标签,并提供自定义预览图上传、URL 设置与恢复 C 站图片。
- **工具箱入口图标可自定义**：设置页支持上传图片、填写 URL、恢复默认菲比图标;已打开的 ComfyUI 页面会通过浏览器存储事件即时同步。
- **递归 LoRA 扫描**（`f83d00e`）：TK 批量 LoRA 加载器和 TK Toolkit 本地管理面板都会读取 LoRA 根目录下的子目录,并保留相对路径。
- **子目录建分类**（`ad3b2b8`）：本地工具箱新增按钮,按一级子目录名称自动创建/复用分类,将对应 LoRA 批量归类;重复点击不会重复添加。
- **跨平台路径**：节点浏览接口增加 `relativePath`,统一用 `/` 表示子目录,并继续兼容 Windows 反斜杠路径。
- `TK String Router`：新增 6 路字符串单选/多选路由,支持接口名称保存和 ComfyUI 风格紧凑面板。
- `TK Danbooru Tag Getter`：兼容 `TAG_BUNDLE` 多选分类,增加正则排除和精准 Tag 排除。
- `TK Prompt Saver`：新增 6 路提示词自动保存,支持对应 `image_1` 到 `image_6` 预览图,写入共享 `anima-lora` Prompt 库。

### 使用说明

- 扫描后点击「子目录建分类」即可处理 `anima base`、`ill`、`sdxl` 等用户自建目录。
- 更深层目录归入一级目录分类;根目录直接放置的 LoRA 保持未分类。

## [2.2.0] - 2026-08-15

### 新增
- **面板新增「服装库」栏目**：带图服装卡片管理（平铺分类、搜索、收藏、增删改查），支持从 Prompt-Manager 云端服装库一键导入（92 张卡片 + 图片随包导入）
- **服装抽卡**：按范围（全部/分类/收藏）随机抽取 1~10 张，支持锁定必选卡、移除、一键复制合并提示词串
- 数据存独立 IndexedDB（clothing-db），不依赖第三方插件云端；支持手动配图，无图显示文字卡

## [2.1.0] - 2026-08-15

### 新增
- **TK 节点系列**：TK Batch LoRA Loader / TK Trigger Words / TK Camera Control（3D 相机画布+预设+自然语言）/ TK Prompt Batch（批量提示词注入）/ TK Text Join
- Prompt Batch：文件选择器、最新文件按钮、自动用最新文件、组独立机位、输出子目录、mute 节点不触发批量
- 面板：命令面板（Ctrl+K）、搜索清除按钮、图标 SVG 统一

### 修复
- 批量节点空组判断（`all(not p.strip())`）、SaveImage 前缀正则（imageSave 类名）、负向注入保持原值
- anima_prompt_parser 相对导入兼容 ComfyUI 包加载

## [2.0.0] - 2026-08-02

本次版本包含大量更新（跨多轮开发）。

### 新增
- **节点 URL 下载**：从 C 站链接下载 LoRA 到本地
  - 支持带/不带 `modelVersionId` 的链接（无则自动取默认版本）
  - 批量下载（每行一个链接）、实时进度条、可取消（自动删除部分文件）
  - 需登录的模型用 **C 站 API Key**（只读权限即可，设置页可统一管理）
  - 自动用 model-versions API 拿正确文件名
- **检查更新**：版本落后时节点工具栏高亮「🔄 有新版本」，点击显示更新指引（git pull / ZIP 覆盖 + 前往 GitHub）
- **节点 LoRA 启用/禁用开关**：关闭的不参与生成但保留在列表（随工作流持久化）
- **组功能**：保存当前列表为新组 / 一键切换 / 删除（合并原「保存组」+「组」按钮）
- **一键复制已启用 LoRA 的触发词**（英文逗号连接）；复制触发词句末带逗号
- **图片解析**：生图模型提取（UNETLoader 等）、卡片切 tab 常驻、一键复制全部 LoRA、完整 lora 名悬浮预览
- **设置页 C 站 API Key 统一管理**；背景图改 IndexedDB 存储（修复大图超限）
- **LoRA 扫描浏览器回退**：不支持 `showDirectoryPicker` 时用文件选择扫描（Firefox/夸克旧版/局域网 IP）
- 顶部菲比图标接入 ComfyUI 标准菜单 API（app.menu）

### 修复
- Outputs 图片闪烁（增量扫描 mtime 毫秒抖动误判 → 加 1s 容差；进入页面重复渲染）
- 复制工作流被忽略问题（区分 UI/API 格式 + API→UI 转换，无 UI chunk 时明确提示）
- C 站下载 401（改用浏览器 UA）、下载文件名错误（用 model-versions API 的 files[0].name）
- 顶栏菲比图标不显示（ICON_URL 路径漏 `web/`）
- 弹窗拖拽选中时鼠标在窗外松开误关（改为按下+松开都在遮罩才关闭）
- LoRA 节点复制粘贴（DOM widget 点击未选中节点）
- Outputs 目录选择兼容提示更明确（localhost/HTTPS + 浏览器）

### 优化
- README 重写（大白话、去 emoji/加粗、删截图展示）
- 「图片 Prompt 解析」更名「图片解析」
- 节点工具栏精简（移除冗余的「刷新列表」、合并「保存组」进「组」）
- 复制触发词支持一键复制全部 + 句末逗号
