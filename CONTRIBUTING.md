# 贡献指南

感谢愿意帮忙！这份文档只说三件事：**改哪里、提到哪个分支、提之前必须做什么**。

---

## ⚠️ 最重要的一条：PR 请提到 `beta` 分支，不要提到 `main`

```
你的 fork  ──PR──▶  Ararararararaki/comfyui-anima-toolkit : beta   ✅ 提这里
                                                              main  ❌ 不要
```

`main` 是**发布分支** —— 它一旦更新，所有装了插件的用户立刻受影响。
`beta` 是**验证区**：改动先落在这里，等维护者跑完完整回归测试再合进 `main`。

**提 PR 时记得把 base 分支从默认的 `main` 改成 `beta`**（这是最容易搞错的一步）。

---

## 一、可以改什么

| 路径 | 内容 | 能改吗 |
|---|---|---|
| `anima_*.py` | 后端节点（Python） | ✅ |
| `web/js/**`、`web/css/**` | 节点内嵌面板（原生 JS + CSS，无构建） | ✅ |
| `panel/src/**` | 独立面板（Vite + TypeScript） | ✅ |
| `README.md`、`docs/FEATURES.md` | 用户文档 | ✅ |
| `app/**` | **面板构建产物** | ❌ **禁止手改** |
| `VERSION`、`CHANGELOG.md` | 版本号与发布日志 | ❌ 维护者统一升版 |
| `data/`、`anima_meta.json`、`panel/node_modules/` | 运行时数据 / 依赖 | ❌ 已在 .gitignore |

> `app/` 是 `panel/src` 编译出来的产物，由 GitHub Actions 在合并后自动重建。
> 手改它 = 下次构建被覆盖，或者更糟：制造冲突。

---

## 二、本地环境

- **Node 20**（只改 `panel/` 才需要）：`cd panel && npm ci`
- **Python 3.11+**（改 `anima_*.py` 需要）
- **ComfyUI**：只有**联调**才需要。纯改面板可以不用。

**只改面板**：`cd panel && npm run dev`（Vite 热更新，接口代理到本地 ComfyUI）。

---

## 三、提交前必须自检（最低门槛）

请在本地至少跑一遍与你改动对应的检查 —— 这是 PR 能被快速合并的前提：

```bash
# 改了 Python 节点
python -m py_compile __init__.py anima_*.py

# 改了节点内嵌面板（web/js）
node --check web/js/<你改的文件>.js

# 改了独立面板（panel/src）
cd panel && npx tsc --noEmit && npm run build:comfyui:ci
```

CI 会在你的 PR 上**自动**跑上面这三类检查（语法 / 类型 / 构建）。**CI 绿了不等于功能正确** —— 它只证明"没写坏"，功能对不对仍需自测与 review。

---

## 四、PR 里请写清楚

1. **改了什么** —— 一句话说清
2. **为什么** —— 解决什么问题 / 满足什么需求
3. **影响面** —— 动了哪些文件、有没有破坏兼容性（比如老工作流还能不能加载）
4. **你怎么验证的** —— 跑过哪些命令、在什么环境下试过

---

## 五、几个必踩的坑（提前说，省你时间）

1. **改了 `.py` 必须重启 ComfyUI 才生效** —— 热重载不会重新导入 Python 模块。
   改 `.js` / `.css` 则刷新浏览器即可（`Ctrl+F5` 强刷）。
2. **画布上的节点可能开着旧版本**：改了节点侧 JS 后，最好**关掉再重开**那个节点，
   否则旧实例的状态会覆盖你的改动。
3. **别把 `app/` 或构建产物一起提交**。
4. **国内网络**：访问 GitHub、下载模型可能需要代理，直连会很慢甚至不通。
5. **ComfyUI 的 Python 控制台报错**是最好的线索 —— 提 issue/PR 时把它贴上。

---

## 六、如果你拿不到 `tests/` 目录

本仓库**有意不发布**开发内容（`tests/`、`tools/`、`AGENTS.md`、带日期的 `HANDOFF-*.md` 等），
目的是让远端只呈现"用户装来用的东西"。

所以从 fork 参与时，你**看不到也跑不了维护者本地的那套测试**。这没关系 ——
上面第三节的自检 + 你自己写的小验证就够了。如果你想要完整的测试套件，可以直接找维护者要一份。

---

## 七、不想走 fork？

如果你已经有本仓库的 Write 权限，也可以直接在 `beta` 分支上开功能分支、推上去、开 PR。
但无论哪种方式，**目标分支都是 `beta`**。
